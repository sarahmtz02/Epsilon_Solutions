<%- include('templates/sidebarMain.ejs') %>
<!-- Si es CL o CLA despliega lo siguiente: -->

<% if (rol == 2 || rol == 3) { %>
    <%- include('templates/sidebarPanelLead.ejs') %>
<% } %>

<%- include('templates/sidebarEnd.ejs') %>

<link rel="stylesheet" href="/css/sidebar.css" />
<link rel="stylesheet" href="/css/section.css" />
<link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css" />

        <main >
            <section class="section-dash-main">
                <h1 id="principal">Resumen de Estado</h1>
                <hr class="db-hr">
                <table class="table">
                    <tr>
                        <th class="bg-danger text-white">Dimensiones</th>
                        <th class="bg-primary text-white">Indicador</th>
                    </tr>
                    <tr>
                        <td>People: </td>
                        <td id="nPeople"><%= nivel_P %></td>
                    </tr>
                    <tr>
                        <td>Craft: </td>
                        <td id="nCraft"><%= nivel_C %></td>
                    </tr>
                    <tr>
                        <td>Business: </td>
                        <td id="nBusiness"><%= nivel_B %></td>
                    </tr>
                    <tr>
                        <td>Nivel General: </td>
                        <td id="nivOverall"><%= Math.min(Number(nivel_P) + 1, Number(nivel_B) + 1, Number(nivel_C)) %></td>
                    </tr>
                </table>
                <br><br>
            </section>
        </main>

    <div class="secondary">
    <aside>
        <section class="section-dash-aside">
            <h1 id="principal" align="center">Boletín</h1>
            <hr class="db-hr">
        </section>
        <section class="section-dash-aside">
            <h1 id="principal" align="center">¿Cómo te sientes el día de hoy?</h1>
            <hr class="db-hr">
        </section>
    </aside>
    </div>

<%- include('templates/footer.ejs') %>

<script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
<script src="/bootstrap/js/bootstrap.min.js"></script>